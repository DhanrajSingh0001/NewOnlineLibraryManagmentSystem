# NewOnlineLibraryManagmentSystem
In-Memory Library Management System in Java using Swing GUI. It manages books, users, and transactions (borrow/return) with data stored in memory via collections. Demonstrates OOP, multithreading, and exception handling, providing a lightweight, thread-safe library management solution.
